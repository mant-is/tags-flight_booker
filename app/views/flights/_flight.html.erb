<%= content_tag :li, flight, id: "flight_#{ flight.id }" do %>
    <label style="vertical-align: middle">
      <%= radio_button_tag :flight, flight.id %>
      <%= flight.from_airport.code %>
      &Rightarrow;
      <%= flight.to_airport.code %>
      &#65292;
      <%= flight.leaves_at.strftime('%l:%m %p') %>
      &#65292;
      <%= flight.start.strftime('%m/%d/%Y') %>
      &#65292;<%= flight.humanize_duration %>

    </label>
<% end %>

