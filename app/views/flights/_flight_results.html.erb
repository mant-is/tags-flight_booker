<%= form_with scope: :booking, url: flights_path, method: :get, local: true do |form| %>
  <% flights.each do |flight| %>
    <%= render flight %>
  <% end %>
  <%= form.submit 'Add passenger details' %>
<% end %>

